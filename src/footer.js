const Footer = () => {
    return ( 
        <div className="footer">
            This is a fake website that was used to show as an example of skill. It is in no way associated with anything real. At all. Except the language 
            and the lessons presented here. Those you can actually learn from. Everything else, nope. 
        </div>
     );
}
 
export default Footer;